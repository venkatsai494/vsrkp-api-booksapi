meta {
  name: OrderUnavailableBook
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/orders
  body: json
  auth: inherit
}

body:json {
  {
    "bookId": {{unAvailableBookId}},
    "customerName": "{{$randomFullName}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
