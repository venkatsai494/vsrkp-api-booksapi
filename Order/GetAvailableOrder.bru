meta {
  name: GetAvailableOrder
  type: http
  seq: 4
}

get {
  url: {{BASE_URL}}/orders/{{orderId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  orderId: 
}

settings {
  encodeUrl: true
  timeout: 0
}
