meta {
  name: GetUnavailableOrder
  type: http
  seq: 6
}

get {
  url: {{BASE_URL}}/orders/{{orderId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  orderId: 
}

settings {
  encodeUrl: true
  timeout: 0
}
