meta {
  name: OrderAvailableBook
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "bookId": {{availableBookId}},
    "customerName": {{$randomFullName}}
  }
}

vars:post-response {
  orderId: res.body.orderId
}

script:pre-request {
  const customerName = faker.person.fullName();
  bru.setEnvVar("customerName", customerName);
}

settings {
  encodeUrl: true
  timeout: 0
}
